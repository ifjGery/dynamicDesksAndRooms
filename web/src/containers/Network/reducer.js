import {LOGIN_USER} from './constants'

const initialState = {
    activePage: null,
    navigationHistory: []
};

export default function(state = initialState, action) {
    switch(action.type) {
        
    }
}